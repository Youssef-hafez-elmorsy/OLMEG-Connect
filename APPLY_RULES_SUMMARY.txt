╔════════════════════════════════════════════════════════════════════════════╗
║              HOW TO APPLY FIRESTORE RULES FOR NOTIFICATIONS                ║
║                                                                            ║
║                    Complete Guide & Quick Reference                        ║
╚════════════════════════════════════════════════════════════════════════════╝

🎯 GOAL: Fix [cloud-firestore/failed-precondition] Error

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚡ QUICK VERSION (5 Minutes)

  1. Go to: https://console.firebase.google.com
  2. Click: OLMEG Connect project
  3. Left sidebar: Firestore Database
  4. Top tabs: Rules
  5. Delete old rules (Ctrl+A, Delete)
  6. Paste new rules (see below)
  7. Click: Publish button
  8. Confirm: Click Publish again
  9. Wait: 1-2 minutes
  10. Restart: flutter run
  11. Test: Go to Notifications tab
  12. ✅ Done!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 DETAILED STEPS

STEP 1: Open Firebase Console
  URL: https://console.firebase.google.com
  Action: Sign in with your Google account

STEP 2: Select Your Project
  Look for: OLMEG Connect
  Action: Click on it

STEP 3: Navigate to Firestore Database
  Left Sidebar:
    Build
    ├── Authentication
    ├── Firestore Database  ← CLICK HERE
    ├── Realtime Database
    ├── Storage
    └── Hosting
  Action: Click "Firestore Database"

STEP 4: Click Rules Tab
  Top Navigation:
    [Data]  [Rules]  [Indexes]  [Usage]
             ↑ CLICK HERE
  Action: Click "Rules"

STEP 5: Clear Old Rules
  Action: 
    1. Click inside the editor
    2. Press Ctrl+A (or Cmd+A on Mac)
    3. Press Delete

STEP 6: Paste New Rules
  Copy this entire code:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FIRESTORE SECURITY RULES (Copy This):

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users collection
    match /users/{userId} {
      allow read, write: if request.auth.uid == userId;
    }

    // Products collection
    match /products/{productId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null && 
        request.resource.data.sellerId == request.auth.uid;
    }

    // Posts collection
    match /posts/{postId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null;
    }

    // Chat collection
    match /chats/{chatId} {
      allow read, write: if request.auth != null && 
        (request.auth.uid == resource.data.buyerId || 
         request.auth.uid == resource.data.sellerId);
    }

    // Notifications collection - THIS FIXES YOUR ERROR
    match /notifications/{notificationId} {
      allow read: if request.auth != null && 
        request.auth.uid == resource.data.userId;
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null && 
        request.auth.uid == resource.data.userId;
    }

    // Favorites collection
    match /favorites/{favoriteId} {
      allow read, write: if request.auth != null && 
        request.auth.uid == resource.data.userId;
    }

    // Ratings collection
    match /ratings/{ratingId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null;
    }

    // Search history
    match /searchHistory/{userId} {
      allow read, write: if request.auth != null && 
        request.auth.uid == userId;
    }
  }
}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 7: Paste Rules
  Action:
    1. Click inside the editor
    2. Press Ctrl+V (or Cmd+V on Mac)
    3. Rules should appear in editor

STEP 8: Click Publish
  Look for: Blue "Publish" button (top-right)
  Action: Click it

STEP 9: Confirm Publication
  Dialog appears: "Are you sure you want to publish these rules?"
  Action: Click "Publish" again to confirm

STEP 10: Wait for Publication
  Status: "Publishing security rules..."
  Wait: 1-2 minutes for completion
  Success: Green checkmark appears

STEP 11: Restart Your App
  Terminal:
    flutter run
  Or:
    1. Close the app
    2. Run: flutter run

STEP 12: Test Notifications
  In App:
    1. Go to Home screen
    2. Click Notifications tab (bottom navigation)
    3. Look for notification badge with count
    4. Should see no errors

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ WHAT GETS FIXED

  ✅ [cloud-firestore/failed-precondition] error
  ✅ Notification badge not showing
  ✅ Can't read notifications
  ✅ Can't create notifications
  ✅ Notifications work properly

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🆘 TROUBLESHOOTING

Problem: Rules won't publish
Solution: 
  • Check for red underlines in editor (syntax errors)
  • Make sure all brackets are closed
  • Copy rules again carefully

Problem: Still getting [failed-precondition] error
Solution:
  • Wait 2-3 minutes for rules to propagate
  • Clear browser cache (Ctrl+Shift+Delete)
  • Restart the app
  • Make sure you're logged in

Problem: Can't find Firestore Database
Solution:
  • Check you're in correct Firebase project
  • Look in left sidebar under "Build" section
  • If not there, create Firestore database first

Problem: Notifications still don't work
Solution:
  • Check that notifications collection exists in Firestore
  • Verify notification documents have "userId" field
  • Make sure you're logged in to the app

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTATION FILES

  ✅ QUICK_REFERENCE_RULES.md - Quick 5-minute guide
  ✅ HOW_TO_APPLY_FIRESTORE_RULES.md - Detailed instructions
  ✅ FIREBASE_CONSOLE_VISUAL_GUIDE.md - Visual step-by-step
  ✅ APPLY_RULES_SUMMARY.txt - This file

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⏱️ TIME ESTIMATE

  Reading this guide: 2 minutes
  Applying rules: 3 minutes
  Waiting for publication: 2 minutes
  Testing: 1 minute
  
  TOTAL: ~8 minutes

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ CHECKLIST

  [ ] Opened Firebase Console
  [ ] Selected OLMEG Connect project
  [ ] Went to Firestore Database
  [ ] Clicked Rules tab
  [ ] Deleted old rules
  [ ] Pasted new rules
  [ ] Clicked Publish
  [ ] Confirmed publication
  [ ] Waited for completion
  [ ] Restarted app
  [ ] Tested notifications
  [ ] ✅ All working!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎉 RESULT

After following these steps:
  ✅ Notifications will work
  ✅ No [failed-precondition] error
  ✅ Badge count will display
  ✅ You can read/create notifications
  ✅ App is ready for production

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Need more help? Check the detailed guides in the documentation files!

